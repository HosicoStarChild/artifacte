<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 400 400" fill="none">
  <!-- Background -->
  <rect width="400" height="400" fill="#0a0a0f"/>

  <!-- Artifacte "A" badge -->
  <rect x="152" y="110" width="96" height="96" rx="16" fill="#c9a55c"/>
  <text x="200" y="178" text-anchor="middle" font-family="Georgia, serif" font-size="58" font-weight="600" fill="#0a0a0f">A</text>

  <!-- Artifacte wordmark -->
  <text x="200" y="240" text-anchor="middle" font-family="Georgia, serif" font-size="32" font-weight="700" font-style="italic" fill="#ffffff">Artifacte</text>

  <!-- No Image Available -->
  <text x="200" y="286" text-anchor="middle" font-family="sans-serif" font-size="17" fill="#666666">No image available</text>
</svg>
